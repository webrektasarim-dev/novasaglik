// Bu script'i çalıştırmak için:
// API endpoint'ini çağır: POST /api/categories/seed

// Alternatif olarak browser console'da:
// fetch('/api/categories/seed', { method: 'POST' }).then(r => r.json()).then(console.log)

console.log('Kategorileri eklemek için:');
console.log('1. Browser console\'da çalıştır:');
console.log('fetch("/api/categories/seed", { method: "POST" }).then(r => r.json()).then(console.log)');
console.log('\n2. Veya admin panelden kategoriler bölümüne git');

